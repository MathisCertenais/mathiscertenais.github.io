# Mathis personal website

A responsive React and TypeScript portfolio built with Vite.

## Local development

```bash
pnpm install
pnpm dev
```

## Quality checks

```bash
pnpm lint
pnpm typecheck
pnpm build
```

The project targets Node.js 24 and is maintained on the `dev` branch.
